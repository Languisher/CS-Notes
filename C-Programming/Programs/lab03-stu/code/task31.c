
// 3.1	求a和b中的最大者。
// (1)一般方法：

#include <stdio.h>

int max(int, int);

int main()
{ 
  int a,b,c;
  scanf("%d %d",&a,&b);
  c=max(a,b);
  printf("a=%d,b=%d,max=%d\n",a,b,c);
  return 0;
} 

int max(int x,int y)
{ 
    int max_number;
    // TODO
   
    return max_number;
}
